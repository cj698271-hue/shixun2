/*
 * Copy this file to local_credentials.h and replace every placeholder locally.
 * local_credentials.h is intentionally ignored by Git and must never be committed.
 */
#ifndef LOCAL_CREDENTIALS_H
#define LOCAL_CREDENTIALS_H

#define WIFI_SSID     "REPLACE_WITH_2_4G_WIFI_NAME"
#define WIFI_PASSWORD "REPLACE_WITH_WIFI_PASSWORD"
#define GEWU_PASSWORD "REPLACE_WITH_DMP_MQTT_PASSWORD"

#endif
